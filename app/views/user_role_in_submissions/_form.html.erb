<%= form_for(@user_role_in_submission_form) do |f| %>
  <% if @user_role_in_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_role_in_submission.errors.count, "error") %> prohibited this user_role_in_submission from being saved:</h2>

      <ul>
      <% @user_role_in_submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if @can_edit_status %>
    <% if current_page?(:controller => :user_role_in_submissions, :action => :new) %>
      <div class="field">
        <%= label_tag 'Username:' %><br>
        <%= text_field_tag 'username' %>
      </div>
    <% end %>
    <div class="field">
      <%= f.label :status %><br>
      <%= f.select :status, UserRoleInSubmissions.statuses.keys %>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
